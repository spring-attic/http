<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>org.springframework.cloud.stream.app</groupId>
        <artifactId>spring-cloud-stream-http-parent</artifactId>
        <version>1.1.0.BUILD-SNAPSHOT</version>
    </parent>

    <modelVersion>4.0.0</modelVersion>

    <artifactId>http-app-generator</artifactId>
    <packaging>pom</packaging>
    <name>http-app-generator</name>

    <properties>
        <bomsWithHigherPrecedence/>
        <bootVersion>1.3.5.RELEASE</bootVersion>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.cloud.stream.app.plugin</groupId>
                <artifactId>spring-cloud-stream-app-maven-plugin</artifactId>
                <version>${scs-app-maven-plugin.version}</version>
                <executions>
                    <execution>
                        <id>app-gen</id>
                        <phase>install</phase>
                        <goals>
                            <goal>generate-app</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <javaVersion>1.7</javaVersion>
                    <bootVersion>${bootVersion}</bootVersion>
                    <generatedProjectHome>${session.executionRootDirectory}/apps
                    </generatedProjectHome>
                    <generatedProjectVersion>${project.version}</generatedProjectVersion>
                    <applicationType>stream</applicationType>
                    <bom>
                        <name>scs-bom</name>
                        <groupId>org.springframework.cloud.stream.app</groupId>
                        <artifactId>http-app-dependencies</artifactId>
                        <version>${project.version}</version>
                    </bom>
                    <additionalBoms>
                        <bom>
                            <name>app-starters-core-dependencies</name>
                            <groupId>org.springframework.cloud.stream.app</groupId>
                            <artifactId>app-starters-core-dependencies</artifactId>
                            <version>1.1.0.BUILD-SNAPSHOT</version>
                        </bom>
                    </additionalBoms>
                    <binders>
                        <kafka/>
                        <rabbit/>
                    </binders>
                    <additionalGlobalDependencies>
                        <dependency>
                            <groupId>org.springframework.cloud</groupId>
                            <artifactId>spring-cloud-starter-config</artifactId>
                        </dependency>
                    </additionalGlobalDependencies>
                    <generatedApps>
                        <http-source/>
                    </generatedApps>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
